version: '3.8'
services:
  app:
    image: nginx:alpine
    ports:
      - "80:80"
    environment:
      - APP_ENV=base
      - APP_ENV=staging
      - DEBUG=false
      - LOG_LEVEL=info
    volumes:
      - ./html:/usr/share/nginx/html
