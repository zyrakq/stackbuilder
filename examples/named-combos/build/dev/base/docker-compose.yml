version: '3.8'
services:
  app:
    image: nginx:alpine
    ports:
      - "80:80"
    environment:
      - APP_ENV=base
      - APP_ENV=development
      - DEBUG=true
      - LOG_LEVEL=debug
    volumes:
      - ./html:/usr/share/nginx/html
